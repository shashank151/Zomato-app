import { ResNameDirective } from './res-name.directive';

describe('ResNameDirective', () => {
  it('should create an instance', () => {
    const directive = new ResNameDirective();
    expect(directive).toBeTruthy();
  });
});
